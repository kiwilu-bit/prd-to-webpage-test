# Project Background

## 项目背景

在当前的前端开发流程中，从产品需求到页面实现通常需要经历「需求文档 → 设计稿 → 开发实现」三个阶段，链路长、协作成本高、迭代周期慢。

本项目旨在探索一种 **AI 驱动的极简交付工作流**：直接将产品 PRD 作为输入，借助 AI 能力和前端组件库，快速生成可交付的页面原型。

---

## 实验目标

验证以下两种工作流在页面生成质量、还原度和效率上的差异：

| 组别 | 工作流 | 预期产出 |
|---|---|---|
| **测试组（1.0）** | PRD → 前端组件库 → 生成页面 | 基于组件库直接生成页面，验证基础可行性 |
| **对比组（2.0）** | PRD → 设计 Skills → 更新后的前端组件库 → 生成页面 | 引入设计规范约束后，验证页面质量提升程度 |

---

## 核心假设

> 在设计 Skills 的约束下（间距、色彩、组件规范等），AI 生成的页面在视觉还原度和一致性上，显著优于无设计约束的直接生成结果。

---

## 参与成员

| 角色 | 成员 |
|---|---|
| 产品 / 设计 | Kiwi Lu |
| 前端开发 | Zhang Ye |
| 技术顾问 | Weida Chen |

---

## 仓库结构（规划）

```
prd-to-webpage-test/
├── requirements/        # 原始 PRD 文档
├── design-skills/       # 设计规范 & Skills 文件
├── output-v1/           # 测试组生成结果（无设计 Skills）
├── output-v2/           # 对比组生成结果（有设计 Skills）
└── evaluation/          # 评估记录 & 对比报告
```

---

## 评估维度（初步）

1. **视觉还原度** — 与设计稿 / 预期效果的接近程度
2. **组件使用准确性** — 是否正确调用了组件库中的对应组件
3. **生成耗时** — 从 PRD 输入到可预览页面的时间
4. **人工修正成本** — 生成后需要手动调整的工作量

---

*本项目处于早期探索阶段，工作流和评估标准将随实验推进持续迭代。*
